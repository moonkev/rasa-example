version: "3.1"

intents:
  - add_numbers
  - greet

entities:
  - operand:
      roles:
        - left
        - right

actions:
  - action_add_numbers

responses:
  utter_greet:
    - text: "Hey! How are you?"

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
